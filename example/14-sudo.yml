tasks:
  # if a command should be run with sudo, flag it as so...
  - cmd: touch /bin/true
    sudo: true

  #... *don't* put sudo in your cmd
  - cmd: sudo touch /bin/true
    stop-on-failure: false

